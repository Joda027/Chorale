# Chaîne de connexion PostgreSQL (Supabase : Project Settings > Database > Connection string > Connection pooling)
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@[HOST]:6543/postgres"

# Chaîne secrète aléatoire pour signer les sessions (ex. générée par : openssl rand -hex 32)
AUTH_SECRET=""
